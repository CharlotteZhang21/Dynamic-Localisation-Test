var VungleAd = {},
    closeRevealDelay = 29e3,
    closeRevealInteractionDelay = 1e3;
VungleAd.adSizes = ["xl", "l", "m", "s", "xs"];
var vungleAd = document.getElementById("vungle-ad");
var vungleTimerInterval, countingDown = 29;

function initVungleAd() { renderVungleAdSizingClass() }

function renderVungleAdSizingClass() { var e = document.getElementById("vungle-ad-container"),
        t = Math.max(e.offsetHeight, e.offsetWidth),
        i = Math.min(e.offsetHeight, e.offsetWidth); "undefined" != typeof theme && null !== theme || (theme = "dark"); var n, s, a = (t - i) / t * 100,
        o = "";
    t === i ? n = "square" : window.innerHeight > window.innerWidth ? (n = "portrait", o = VungleAd.adSizes[1 * Math.ceil(a / 10) - 1] ? " portrait-" + (s = VungleAd.adSizes[1 * Math.ceil(a / 10) - 1]) : " portrait-" + (s = VungleAd.adSizes[VungleAd.adSizes.length - 1]) + " oob") : (n = "landscape", o = VungleAd.adSizes[1 * Math.ceil(a / 10) - 1] ? " landscape-" + (s = VungleAd.adSizes[1 * Math.ceil(a / 10) - 1]) : " landscape-" + (s = VungleAd.adSizes[VungleAd.adSizes.length - 1]) + " oob"), adClassName = n.concat(o), document.body.className = adClassName + " " + theme + " " + getOS(), VungleAd.shape = n, VungleAd.sizeClass = s, VungleAd.theme = theme, VungleAd.os = getOS();
    document.getElementById("vungle-close"); if ("undefined" != typeof closeButtonTimer && null !== closeButtonTimer) switch (closeButtonTimer) {
        case "NOTIMER":
            revealCloseButton(); break;
        case "TIMER":
            setTimeout(revealCloseButton, closeRevealDelay), document.addEventListener("click", function() {}); break;
        default:
            revealCloseButton() } else revealCloseButton() }
function startCountingDownTimer() {
	document.getElementById("close-button-replacement").className = "";
	vungleTimerInterval = setInterval(function(){
		document.getElementById("close-button-replacement").innerHTML = countingDown--;
	}, 1e3);

}
function revealCloseButton() { clearInterval(vungleTimerInterval); document.getElementById("vungle-close").className = "" , document.getElementById("close-button-replacement").className = "hide";}

function callSDK(e) { return actionClicked(e) }

function addClass(e, t) { e.classList ? e.classList.add(t) : e.className += " " + t }

function removeClass(e, t) { e.classList ? e.classList.remove(t) : e.className = e.className.replace(new RegExp("(^|\\b)" + t.split(" ").join("|") + "(\\b|$)", "gi"), " ") }

function hasClass(e, t) { return (" " + e.className + " ").indexOf(" " + t + " ") > -1 }

function getOS() { var e = window.navigator.userAgent,
        t = window.navigator.platform; return -1 !== ["Macintosh", "MacIntel", "MacPPC", "Mac68K"].indexOf(t) || -1 !== ["iPhone", "iPad", "iPod"].indexOf(t) ? "ios" : -1 !== ["Win32", "Win64", "Windows", "WinCE"].indexOf(t) ? "windows" : /Android/.test(e) ? "android" : "" }

function greatestCommonDivisor(e, t) { return 0 == t ? e : greatestCommonDivisor(t, e % t) }

function vungleVideoType(e, t) { var i, n = greatestCommonDivisor(e, t); switch (e / n + ":" + t / n) {
        case "16:9":
            i = "landscape"; break;
        case "9:16":
            i = "portrait"; break;
        case "1:1":
            i = "square"; break;
        default:
            i = "nonconform" } return { ratio: e / n + ":" + t / n, classname: "ratio" + e / n + t / n, type: i, width: e, height: t } }

function resizeVideo(e, t, i) { var n = .5625 * e.offsetHeight,
        s = e.offsetWidth / .5625,
        a = e.offsetHeight / .5625,
        o = .5625 * e.offsetWidth,
        l = e.offsetHeight,
        d = e.offsetWidth;
    t.style.width = "", t.style.height = "", hasClass(document.body, "portrait") || hasClass(document.body, "square") ? "9:16" === vungleVideo.ratio ? n <= e.offsetWidth ? t.style.width = n + "px" : t.style.height = s + "px" : "16:9" === vungleVideo.ratio ? o <= e.offsetHeight ? t.style.height = o + "px" : t.style.width = a + "px" : l <= e.offsetWidth ? t.style.width = l + "px" : t.style.height = d + "px" : "9:16" === vungleVideo.ratio ? s <= e.offsetHeight ? t.style.height = s + "px" : t.style.width = n + "px" : "16:9" === vungleVideo.ratio ? o <= e.offsetHeight ? t.style.height = o + "px" : t.style.width = a + "px" : l <= e.offsetWidth ? t.style.width = l + "px" : t.style.height = d + "px" } window.addEventListener("load", function() { initVungleAd(); startCountingDownTimer(); }), document.ontouchmove = function(e) { e.preventDefault() }, window.addEventListener("resize", function(e) { vungleAd.style.opacity = 0, this.resizeTimer && clearTimeout(this.resizeTimer), this.resizeTimer = setTimeout(function() { renderVungleAdSizingClass(), vungleAd.style.opacity = 1 }, 200) });